spring.application.name=project-proposal-service
server.port=${PORT:8080}

# ===== DynamoDB =====
app.aws.region=us-east-1
# Optional: use this only for DynamoDB Local/testing
# app.dynamodb.endpoint=http://localhost:8000

# ===== JWT =====
# NOTE: set a real secret in production (min 32 chars). Example env: APP_JWT_SECRET
app.jwt.secret="your-very-long-secret-key-at-least-32-characters-long"
app.jwt.issuer=project-proposal-service
app.jwt.expMinutes=120

